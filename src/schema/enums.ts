export enum EnumEmployeeRole {
  STAFF = "STAFF",
  ADMIN = "ADMIN",
  MANAGER = "MANAGER",
}

export enum EnumItemStatus {
  AVAILABLE = "AVAILABLE",
  CHECK_OUT = "CHECK_OUT",
  MAINTENANCE = "MAINTENANCE",
  LOST = "LOST",
  DAMAGE = "DAMAGE",
}

export enum EnumReturnItemStatus {
  AVAILABLE = "AVAILABLE",
  LOST = "LOST",
  DAMAGE = "DAMAGE",
}

export enum EnumLoanFees {
  BOOK = 0,
  DVD = 3,
  SOFTWARE = 6,
}

export enum EnumLoanPeriodDays {
  BOOK = 28,
  DVD = 14,
  SOFTWARE = 30,
}
